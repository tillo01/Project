/**
 * PM2 Commands
 * 1. pm2 ls
 * 2. pm2 start dist/server.js --name=AURA
 * 3. pm2 start npm --name "AURA1" -- run start:prod
 * 4. pm2 start "npm run start:prod"
 * 5. pm2 stop id(1 yoki 2)
 * 6. pm2 delete id
 * 7. pm2 restart id
 * 8. pm2 monit
 *
 * bin bash
 * chmod +x deploy.sh
 * chown +x deploy.sh
 *
 * @format
 */
